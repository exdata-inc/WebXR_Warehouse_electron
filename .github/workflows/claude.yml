name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

permissions:
  contents: write # ブランチを push するため
  issues: write # Issue にコメントするため
  pull-requests: write # PR を作る / 閲覧するため（write は read を含む）
  actions: read # Actions の実行結果・ログ・アーティファクトを見るため（gh run list / view / --log）
  checks: read # PR の CI チェック結果を見るため（gh pr checks / check runs API）
  statuses: read # commit status ベースの CI 結果を見るため
  id-token: write

env:
  CLAUDE_CODE_ENABLE_TELEMETRY: "1"
  OTEL_METRICS_EXPORTER: "otlp"
  OTEL_LOGS_EXPORTER: "otlp"
  OTEL_EXPORTER_OTLP_PROTOCOL: "http/protobuf"
  OTEL_EXPORTER_OTLP_ENDPOINT: "https://otlp-gateway-prod-ap-southeast-0.grafana.net/otlp"
  OTEL_EXPORTER_OTLP_HEADERS: "Authorization=Basic Njc0MTA2OmdsY19leUp2SWpvaU9EZ3pOVE0ySWl3aWJpSTZJbk4wWVdOckxUWTNOREV3TmkxcGJuUmxaM0poZEdsdmJpMXZjbUZqYkdVdFpYaGtZWFJoTFdOcFlTMHhJaXdpYXlJNklqRjVlVFl5TjFjNE4wVTRSMDlWWmxneWNuazJlVFpUYnlJc0ltMGlPbnNpY2lJNkluQnliMlF0WVhBdGMyOTFkR2hsWVhOMExUQWlmWDA9"
  OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE: "cumulative"
  OTEL_RESOURCE_ATTRIBUTES: "adhd_user=gh-claude"

jobs:
  claude-code:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: self-hosted
    # rate limit 時に resetsAt(最大 5h 先)まで待ってから --resume で再開するため、
    # デフォルト 360 分では「実行 120 分 + 待機 + 再開 120 分」が収まらない。余裕を持って 12h。
    timeout-minutes: 720
    container:
      image: ngtystr/rust-dev-env:1.93.0-21.1.8-20260827
    # この container では Actions runner が bash を自動検出できず sh フォールバックしていたため、
    # ジョブ全体の run step を bash で実行するよう明示する。bash は image に同梱されている前提。
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # fetch-depth: 1（shallow）だと claude が `git log` / `git blame` /
          # `git diff <base>...HEAD` / rebase を実行できず、「履歴が見られない＝
          # 権限が無い」と誤解して調査を諦めることがあった。opportunistic-task.yml と
          # 揃えて全履歴を取る（Cleanup Directory でどのみち毎回消えるので差は初回 fetch のみ）。
          fetch-depth: 0

      - name: Prepare task environment
        run: |
          set -euo pipefail
          git config --global --add safe.directory '*'

      # Slack スレッドの添付を claude が Read できるよう、実行前に取得しておく。
      # slackmeta(channel/ts) はハブから引き継がれるので、各 repo 側でも同じ添付を再取得できる。
      # 画像は claude が Read で視覚的に解析できる。失敗してもジョブは止めない（exit 0）。
      - name: Download Slack attachments
        if: contains(github.event.issue.body, 'slackmeta')
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          set -uo pipefail
          META=$(grep -oP '(?<=slackmeta ).*(?= -->)' <<< "$ISSUE_BODY" || true)
          if [ -z "$META" ]; then echo "slackmeta なし。スキップ"; exit 0; fi
          CH=$(jq -r .channel <<< "$META"); TS=$(jq -r .ts <<< "$META")
          mkdir -p slack-attachments
          resp=$(curl -s -G https://slack.com/api/conversations.replies \
            -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
            --data-urlencode "channel=$CH" --data-urlencode "ts=$TS" --data-urlencode "limit=1000")
          if [ "$(jq -r '.ok' <<< "$resp")" != "true" ]; then
            echo "conversations.replies 失敗: $(jq -r '.error // "unknown"' <<< "$resp")" >&2
            echo "（Slack アプリに files:read と channels:history / groups:history スコープが必要）" >&2
            exit 0
          fi
          count=0
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            id=$(jq -r '.id' <<< "$f")
            name=$(jq -r '.name // "file"' <<< "$f")
            url=$(jq -r '.url_private_download // .url_private // empty' <<< "$f")
            [ -z "$url" ] && continue
            safe=$(basename "$name" | tr -cd 'A-Za-z0-9._-'); [ -z "$safe" ] && safe="file"
            out="slack-attachments/${id}_${safe}"
            if curl -sfL -H "Authorization: Bearer $SLACK_BOT_TOKEN" "$url" -o "$out"; then
              echo "✓ $name -> $out"; count=$((count + 1))
            else
              echo "✗ DL 失敗: $name" >&2
            fi
          done < <(jq -c '.messages[]?.files[]?' <<< "$resp")
          jq -r '.messages[]?.files[]? | "- \(.name)  (\(.mimetype // "?"), \(.size // 0) bytes)"' <<< "$resp" > slack-attachments/INDEX.md || true
          echo "添付 $count 件を slack-attachments/ に保存しました"
          ls -la slack-attachments || true

      # ANTHROPIC_BASE_URL / ANTHROPIC_AUTH_TOKEN が repo の Actions secrets / variables に設定されていれば実行時の環境へ反映する
      #（独自の Anthropic 互換エンドポイント＝gateway やローカル wrapper へ向けたいとき用）。
      # 未設定なら何も足さない（= 既定の OAuth フローのまま）。
      # 空文字で既定を上書きしないよう、値があるときだけ書く。
      - name: Apply Anthropic env overrides (if set)
        env:
          IN_ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL || vars.ANTHROPIC_BASE_URL }}
          IN_ANTHROPIC_AUTH_TOKEN: ${{ secrets.ANTHROPIC_AUTH_TOKEN || vars.ANTHROPIC_AUTH_TOKEN }}
        run: |
          set -euo pipefail
          if [ -n "${IN_ANTHROPIC_BASE_URL:-}" ]; then
            echo "ANTHROPIC_BASE_URL=${IN_ANTHROPIC_BASE_URL}" >> "$GITHUB_ENV"
            echo "ANTHROPIC_BASE_URL を反映しました: ${IN_ANTHROPIC_BASE_URL}"
          fi
          if [ -n "${IN_ANTHROPIC_AUTH_TOKEN:-}" ]; then
            echo "ANTHROPIC_AUTH_TOKEN=${IN_ANTHROPIC_AUTH_TOKEN}" >> "$GITHUB_ENV"
            echo "ANTHROPIC_AUTH_TOKEN を反映しました: ${IN_ANTHROPIC_AUTH_TOKEN}"
          fi
          true

      - name: Run Claude Code
        id: claude-code
        uses: ngtystr/claude-code-action@main
        with:
          use_oauth: "true"
          claude_access_token: ${{ steps.claude-oauth.outputs.access_token || env.ANTHROPIC_AUTH_TOKEN || '' }}
          claude_refresh_token: "disabled"
          claude_expires_at: ${{ steps.claude-oauth.outputs.expires_at || '9999999999999' }}
          # 長時間実行中に上の access token が期限切れになる対策。URL 自体を渡しておくと
          # action 側が (1) 期限が近づいたら再取得して ~/.claude/.credentials.json を
          # 書き換え、(2) それでも認証エラーで落ちたら再取得 + --resume で同一セッションを
          # 再開する（rate limit retry と同じ仕組み。max_retries を消費する）。
          claude_oauth_url: ${{ secrets.CLAUDE_OAUTH_URL }}
          model: ${{ secrets.ANTHROPIC_MODEL || vars.ANTHROPIC_MODEL || vars.CLAUDE_MODEL || 'claude-sonnet-5' }}
          # モデル別の限度切れ対策(例: claude-fable-5 の限度だけ先に尽きるケース)。
          # 上の model を起点に、rate limit / モデル利用不可(404)を踏んだら序列のより
          # 下位モデルへ --resume で切り替えて作業を継続する(セッション・会話は引き継ぐ)。
          # 序列は「上位 → 下位」。model が序列内にあればその位置から下だけを使い、
          # 無ければ model の次に序列の先頭から続く。リセット時刻まで待った後は序列の
          # 先頭(= 起点モデル)に戻る。新モデルが出たらここに追記するだけでよい。
          model_fallback_chain: |
            claude-fable-5
            claude-opus-5[1m]
            claude-opus-5
            claude-opus-4-8[1m]
            claude-opus-4-8
            claude-sonnet-5
            claude-haiku-4-5
          # claude-code-action の実行時間制限（デフォ 30 分 → 720 分に延長）。
          timeout_minutes: '720'
          # rate limit 対策の retry。stream-json の rate_limit_event を構造的に検知し、
          # resetsAt(リセット時刻)まで一括待機 → `--resume <session-id>` で同一セッションを
          # 再開する。会話コンテキストが引き継がれるので、中断までに使ったトークン
          # (数十ターン分の作業)が無駄にならない。resetsAt が取れない失敗は 60s 間隔で
          # 再試行(上限 120 回)。rate limit 以外の失敗は従来どおり即 fail。
          # 単発待機の上限はデフォルト 8h(retry_max_wait_seconds)。job の timeout-minutes: 720
          # とセットで運用する。
          max_retries: '120'
          retry_interval_seconds: '60'
          # CLAUDE.md の手順に必要なツールを許可する（CI では未許可ツールは弾かれるため）。
          #
          # action が常に付ける「ベース許可」は下記だけ:
          #   Edit / MultiEdit / Glob / Grep / LS / Read / Write /
          #   mcp__github_file_ops__{commit_files,delete_files,update_claude_comment}
          # ここに書いたものが追加される。つまり **書かなかったツールは実行時に denied** になる。
          #
          #   Bash        : post_slack ヘルパー（curl/jq/grep）、ビルド/テスト、gh での PR 作成に必須
          #   WebFetch    : URL 明示のページ取得（ベースでは disallowed 側に入っているので明示が必要）
          #   WebSearch   : エラー文言などの Web 検索（同上）
          #   Skill       : ~/.claude/skills 配下のスキル起動
          #   SlashCommand: スキル/コマンドの呼び出し
          #   NotebookEdit: .ipynb を含む repo での編集
          #   mcp__github_file_ops__create_pull_request
          #               : **PR 作成の本体**。これが無いと「PR を作れない」と言って
          #                 Create-a-PR リンクを貼るだけで終わっていた（実際に多発）。
          #   mcp__github_file_ops__{create_issue,update_issue_comment,list_issues}
          #               : 追跡 Issue の起票・コメント更新・関連 Issue の確認
          # ※ コード修正・PR 作成が役割なので commit/PR 系は禁止しない。
          allowed_tools: |
            Bash
            WebFetch
            WebSearch
            Skill
            SlashCommand
            NotebookEdit
            mcp__github_file_ops__create_pull_request
            mcp__github_file_ops__create_issue
            mcp__github_file_ops__update_issue_comment
            mcp__github_file_ops__list_issues
          # ★ここが「権限不足で何もしてくれない」の最大の原因だった★
          # claude-code-action の内蔵システムプロンプトには CAPABILITIES AND LIMITATIONS 節があり、
          # 「What You CANNOT Do」として下記が **ハードコードで** 並んでいる:
          #   - Run arbitrary Bash commands (unless explicitly allowed via allowed_tools configuration)
          #   - View CI/CD results or workflow run outputs (cannot access GitHub Actions logs or test results)
          #   - Perform branch operations (cannot merge branches, rebase, ...)
          # さらに issue 起点の実行では「PR を作る代わりに Create a PR リンクを貼れ」と指示される。
          # これは GitHub App + GitHub ホストランナー前提の汎用文面で、このジョブの実態
          #（Bash 全面許可 / actions:read 付与 / gh 同梱 / self-hosted コンテナで root）とは合っていない。
          # そして claude はこの文面を読んで素直に「権限がないのでできません」と諦めてしまう。
          # custom_instructions はプロンプトの **最後** に追記されるので、ここで実態を上書きする。
          custom_instructions: |
            ## 実行環境と権限の実態（内蔵プロンプトの CAPABILITIES AND LIMITATIONS 節より優先すること）

            このジョブは self-hosted runner の Docker コンテナ内で **root** として動いています。
            下記はすべて実行できます。「権限が無い」と言って諦めないでください。

            - **Bash は全面的に許可済み**です。任意のコマンド・任意のスクリプトを実行してよい。
              コンテナに同梱済み（すべて PATH 済み）:
              `python` / `python3` / `pip`（/opt/venv）、`node` / `npm`、`bun`、`cargo` / `rustc` / `clang`、
              `java` / `mvn` / `gradle`、`gh`、`git`、`jq`、`curl`、`make` / `cmake`、`sudo`、`brew`(linuxbrew)。
              足りないものは `pip install` / `npm i` / `brew install` で入れてよい（ネットワークは使える）。
            - **テスト・ビルド・lint は必ず実行して確認**すること。「実行できないので未検証」で終えない。
            - **GitHub Actions のログ・CI 結果は見られます**（このジョブに actions:read / checks:read /
              statuses:read が付いています）。`gh run list` / `gh run view --log` / `gh pr checks` /
              `gh api` を使ってよい。内蔵プロンプトの「CI の結果は見られない」は誤りです。
            - **git のブランチ操作は可能**です（branch / checkout / rebase / cherry-pick / push）。
              ただし CLAUDE.md の禁止事項（merge しない・デフォルトブランチへ直 push しない・
              force push しない）は厳守すること。
            - **PR は自分で作ってください。** 「Create a PR」リンクを貼って終わりにしないこと。
              `gh pr create --base <デフォルトブランチ> --head <作業ブランチ> --title ... --body ...`
              または `mcp__github_file_ops__create_pull_request` を使う。
              作成できたら PR の URL を必ずコメントと Slack に残すこと。

            ## 本当に存在する制限
            - `.github/workflows/` 配下の変更は GITHUB_TOKEN では push が拒否されます
              （workflow 権限が無いため）。ワークフローの修正が必要だと判断したら、
              差分を提案としてコメントに書き、人間にメンションして終えること。
            - merge・PR の approve は行いません（人間の仕事）。

            ## それでも本当にツールが denied されたとき
            「権限がありませんでした」で終わらせないこと。**弾かれたツール名と実行しようとした
            コマンドをそのまま**コメントに書いてください（例: `Bash(pytest -q)` が denied、
            `mcp__github_file_ops__xxx` が denied）。ワークフローの `allowed_tools` を直す
            手掛かりになります。
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          # claude の Bash 内で `gh` を認証させるためのトークン（PR 作成/コメント等）。
          # GITHUB_TOKEN でも同一 repo の PR 作成は原則可能だが、下記 2 点で詰まることがある:
          #   1. Settings → Actions → 「Allow GitHub Actions to create and approve pull requests」が
          #      OFF だと `gh pr create` が 403（"GitHub Actions is not permitted to create ...
          #      pull requests"）で落ちる。org 既定が OFF のことが多い。
          #   2. GITHUB_TOKEN 作成の PR は他ワークフロー（claude-code-review 等）を起動しない。
          # そこで PAT（secrets.GH_DISPATCH_TOKEN）があればそちらを優先する。未設定なら従来どおり
          # GITHUB_TOKEN で動く（後方互換）。1 に該当して PR が作れない repo は、この secret を
          # 入れるか上記設定を ON にすること。
          GH_TOKEN: ${{ secrets.GH_DISPATCH_TOKEN || github.token }}
          # submodule 内のコード修正まで踏み込む必要が出たとき、claude が git submodule update /
          # commit / push に使うトークン（CLAUDE.md の "submodule を操作するときのお願い" 節参照）。
          # secrets に未登録なら空文字が渡り、claude はその場合「権限外」と判断してコメントで終える。
          GH_TOKEN_SUBMODULE_RW: ${{ secrets.GH_TOKEN_SUBMODULE_RW }}

          # Optional: Customize the trigger phrase (default: @claude)
          # trigger_phrase: "/claude"

          # Optional: Trigger when specific user is assigned to an issue
          # assignee_trigger: "claude-bot"

          # Optional: Allow Claude to run specific commands
          # allowed_tools: "Bash(npm install),Bash(npm run build),Bash(npm run test:*),Bash(npm run lint:*)"

          # Optional: Add custom instructions for Claude to customize its behavior for your project
          # custom_instructions: |
          #   Follow our coding standards
          #   Ensure all new code has tests
          #   Use TypeScript for new files

          # Optional: Custom environment variables for Claude
          # claude_env: |
          #   NODE_ENV: test

      # このジョブはコンテナ内で root として動くため、checkout / cargo が生成した
      # ファイルは host 上で root 所有になる。次に同じ self-hosted runner で
      # コンテナを使わないワークフロー(main 等)が動くと、runner ユーザーがそれらを
      # 削除できず checkout(git clean) が "permission denied" でコケる。
      # そこで (1) 所有者をマウント元 /__w(= runner ユーザー)に戻し、(2) 中身を削除する。
      # ビルドが途中で失敗しても必ず後始末されるよう if: always() を付ける。
      - name: Cleanup Directory
        if: always()
        run: |
          chown -R --reference=/__w /__w 2>/dev/null || true
          rm -rf -- "$GITHUB_WORKSPACE"/* "$GITHUB_WORKSPACE"/.[!.]* "$GITHUB_WORKSPACE"/..?* 2>/dev/null || true
